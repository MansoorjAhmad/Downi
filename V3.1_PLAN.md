# DOWNI v3.1.0 — "THE POLISH RELEASE" — Execution Plan

> Same brand. Same design. Same engine. Perfect feel.
> Vault becomes silky. DowniDrop becomes magic again.

**Locked decisions (user GO):**
1. v3.0.4 (unpushed commit 8347585) folds into v3.1.0 → versionCode 45 / versionName 3.1.0
2. Vault: **Phase 1 ONLY** (Phase 2 — shimmer, content-visibility, press-scale — parked for v3.2)
3. DowniDrop 2.0: v2.6.4 invisible-grabber architecture restored + v3.0 upgrades (BEST PICK memory, queue, custom folder, rich notifications) + Settings toggle (Instant default / Ask quality)

## Workstream A — Vault Perfection (`www/index.html`)
- A1: Keyed-DOM reconciliation (`Map<key, element>`) — NO `grid.innerHTML` rebuilds. Root cause of blink.
- A2: In-place selection mode (classList toggle, no re-render)
- A3: Smart refresh (signature compare → no-op when unchanged)
- A4: New-arrival single-card animation only
- A5: 200ms search debounce

## Workstream B — DowniDrop 2.0
- B1: NEW `DropActivity.java` (transparent, noHistory, excludeFromRecents) — reads toggle, instant grab → Toast + `DowniDownloadService.startShared()`, else → Inspector in MainActivity
- B2: Restore `DowniDownloadService.startShared()` — **includes Python self-start** (`Python.isStarted()` → `Python.start()`), v2.6.4 pattern. Current service is notifications-only (zero Python refs) — must be rebuilt, NOT assumed.
- B3: Settings toggle in `www/index.html` (Instant / Ask quality), synced to SharedPreferences via `DowniEnginePlugin`
- B4: Manifest: SEND/PROCESS_TEXT filters move MainActivity → DropActivity

## Workstream C — Release
- C1: `npx cap sync android`, smoke test
- C2: versionCode 45 / versionName "3.1.0"
- C3: CHANGELOG, README, HANDOFF, ROADMAP
- C4: Device test (vivo V2058, USB) — incl. **cold-start share with process killed** (Checkpoint 2 test, per review)
- C5: commit + tag v3.1.0 + push → GitHub Actions → signed APK → in-app updater

## Rules (unchanged)
- No client spoofing, no Instagram sessions, no applicationId/signing change
- No extraction-layer changes; yt-dlp pin 2026.8.19 untouched
- 100% vanilla JS, no new dependencies

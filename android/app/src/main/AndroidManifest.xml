<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- DowniDrop 2.0: shared links land in the invisible DropActivity (v2.6.4
             pattern, restored). Shares ALWAYS grab instantly in the background — the
             Instant/Ask-quality toggle was retired in v3.1.1 (owner ruling 2026-09-24).
             taskAffinity="" keeps DropActivity out of DOWNI's own task: without it, a
             warm share (app sitting in recents) dragged DOWNI's whole task — MainActivity
             included — to the front, so the user "left" the platform app. Cold shares
             never showed it because the throwaway task simply died with the activity.
             (Warm-share redirect bug, reported on device 2026-09-24.) -->
        <activity
            android:name=".DropActivity"
            android:theme="@style/AppTheme.ShareReceiver"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <service
            android:name=".DowniDownloadService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- V3.2 Phase 0 detectability spike (owner-approved 2026-09-24): logs what
             Instagram/TikTok expose on this device (files/fetch-spike/, local only).
             Debug tool — no bubble, no auto-download, never part of a release build.

             The intent action MUST be android.accessibilityservice.AccessibilityService —
             the FQCN that AccessibilityService.SERVICE_INTERFACE resolves to, which is
             what AccessibilityManagerService queries when it builds the installed-service
             list. An earlier revision declared android.service.accessibility.* (wrong word
             order); the service was then never discovered, never listed in Settings and
             never bound — on any device, whatever else looked correct. -->
        <service
            android:name=".FetchSpikeService"
            android:label="DOWNI Fetcher Spike"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/fetch_spike_service" />
        </service>

        <!-- Quick Settings tile: one tap into DOWNI's Grab screen -->
        <service
            android:name=".GrabTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_grab"
            android:label="DOWNI — Grab"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
</manifest>
